# 0.1.1 Android 商店包体积核查 · 2026-09-23

核查对象为 `artifacts/release-0.1.1-final-2/build/android/com.companyname.hanmate.app-Signed.aab`，SHA256 `FA87B74E7E3AFA816E8DC4C514DDC92DDD5C5A6E81667B8BE429B44AE166D4AE`，文件长 779,150,195 字节。对应 `manifest.json` 的源提交为 `a3e9f60e2ccf2ce9c7b0a2e7ed033088c09e6e7c`。这是现有开发签名候选包的只读 ZIP 条目统计，不代表商店接受、下载量或设备安装占用。

| AAB 条目组 | 压缩后 MiB | 主要内容 |
|---|---:|---|
| Kokoro | 339.8 | `model.onnx` 287.6、`voices.bin` 38.0 |
| Melo | 156.9 | `model.onnx` 150.5 |
| 原生库 | 111.6 | `libassembly-store.so` 95.1，含托管资源；不能直接把整项认作运行时开销 |
| 其他资源 | 94.0 | WordAudio 76.1、Pinyin 17.7 |
| 手写资源 | 30.6 | `templates.bin` 28.2 |
| 其他 | 8.1 | DEX、清单等 |

`base` 模块各条目压缩后合计约 739.6 MiB。仅去掉 Kokoro 和 Melo 两组后仍约 242.9 MiB；这两个模型之外，录音、内嵌字典和手写资产也需要纳入商店交付方案。下一步应先确定适用于 .NET MAUI 的商店资源交付方式并做可安装样包，保持默认离线能力、已有模型选择和升级资料的要求；若选择改成首次使用下载，需单独记录用户体验变化、固定来源/哈希、失败重试和隐私文案。不能把临时排除语音模型的模拟器 UI 测试包当作正式商店包。

本次未改变随包资源或 0.1.1 发布资产；Google Play 对该包的当前上传结果仍需在控制台实际验证。
