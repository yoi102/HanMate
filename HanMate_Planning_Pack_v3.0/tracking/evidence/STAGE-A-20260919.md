# 阶段 A：拼音与离线语音收口 · 2026-09-19

范围：执行 [COMPLETION_PLAN](../COMPLETION_PLAN.md) 的 A1—A6 本机任务。iOS 按用户要求暂缓；Windows UI、真实满盘、正式听审/素材授权仍未关闭。DONE 保持 35/48。

## 实现修复

- 语音包使用 `ResponseHeadersRead`，原 `HttpClient.Timeout` 只覆盖响应头，正文可能无限停滞。每次读取增加默认 30 秒无数据超时，失败清理本次暂存、可重试；网络超时与用户取消分别处理。语音页提供中日英超时提示。
- 重试安装只清理 `stage-<32位GUID>` 的本模块中断目录，保留其他目录。下载仍按固定文件大小/SHA256 验证，全量完成才发布，读取租约期间不卸载。
- AI 预检原按180元素分块，实际播放按48元素分块，长标点可能先播放一段再失败。阅读后端、预检和试听统一使用 `OfflineVoiceInput.Chunks` 的48元素规则；空白块一致跳过，任何不可合成块在队列预检时拒绝，未改人工拼音或教学录音优先规则。

## A1 台账与 A4 审核资料

- 新建 [执行规则](../EXECUTION_LEDGER.md)、[逐平台台账](../execution-ledger.csv) 和9项 [补充用例](../acceptance-supplement.csv)。保留原176项定义和聚合状态，明确区分Shared逻辑、原生合成、平台UI及部分覆盖。补充用例不增加原48工作包计数。
- 离线生成63教学项的直接示范/例字/拼音映射，以及200段音频逐文件哈希、作者、来源和许可清单，全部哈希匹配。[审核入口与录制规格](../review/README.md)。
- 本次核查：audio-cmn固定树没有独立ong和LICENSE，GitHub license API 404；SWAC原链接DNS失败；Commons音频查询前30项未发现可确认的普通话独立ong。没有用外语同名音或dong替代。
- 原录音CC BY-SA版本仍未明确，200段均不记终审通过。已准备独立ong原创录制与署名授权规格；需要原作者授权确认或有明确许可的替换素材。
- 固定10句听审材料覆盖多音字、轻声、ü、日期/小数、号码、长数字和标点。最终生产合成器为067/011生成22段WAV及SHA256，位置为本机TEMP/HanMate-voice-probe/review-samples；全部信号可用，**人工听审 NOT RUN**，不能称推荐音色已经审核。生成清单的可移植副本见本目录 `STAGE-A-review-samples-20260919.json`。

## 自动验证与构建

| 范围 | 结果 | 说明 |
|---|---|---|
| Core受影响测试 | PASS 40/40 | 分块/输入校验、播放协调器和课程；包含新增长标点预检、空白块与尾部未知字场景 |
| Infrastructure受影响测试 | PASS 29/29 | VoicePack/ReadingAudio/Speech；其中VoicePack14项覆盖断流/正文停滞/头超时/取消/损坏/重试/持久选择/使用租约/写失败 |
| 最终原生预检与听审样本生成 | PASS | 长标点块在原生前拒绝、空行长文本预检通过；22段真实生成。不是听审结论 |
| Windows Release | PASS | 0警告0错误，最终程序集SHA256见台账/下文 |
| Android Release及覆盖安装 | PASS | 0警告0错误，`adb install -r`成功，没有清库或替换真实数据 |
| APK隐私/权限预检 | PASS | INTERNET/RECORD_AUDIO范围、关闭自动备份、分享目录；518键三语言一致 |
| 正式发行 | BLOCKED | 身份/发行签名与完整原生/内容审核未齐，发布预检保持阻塞 |

上述69项是本轮受影响测试，不是重新执行原463项全套。TRX的摘要和被测程序集哈希见 `STAGE-A-verification-20260919.json`。写入失败测试在隔离暂存目录制造实际文件系统拒绝；**不是手机存储卷真实满盘测试**。下载故障使用受控HTTP handler，不声称实际CDN已模拟每种断网情况。

原生探针运行中曾向同一输出目录重新构建而遇到DLL占用（MSB3021/MSB3027）；未关闭稳定性进程，改用独立TEMP输出目录后最终探针构建零警告错误，生成与预检回归通过。不是未修复的源码编译错误。

## A3 原生20分钟稳定性结果

`STAGE-A-native-soak-20260919.json` 保存逐轮采样：1200.70秒、1010轮、30次取消并恢复，全部合成/信号检查通过。每轮完整工程输入（长数字可能含多个48元素块）耗时P50 613.81ms、P95 2521.04ms，最长2743.76ms；取消观察最长545.80ms。这不是设备首音延迟。

预热3分钟后私有内存采样64.28—120.66MiB；第3—8分钟均值113.12MiB、最后5分钟114.84MiB。本次窗口未见持续失控增长，不推断长期真机无泄漏。期间存在开发构建/设备验证负载，数据用于稳定性观察，不与独占环境性能基线比较。

此20分钟进程启动于统一预检分块修复之前，使用相同的48元素合成输入及未改变的原生生成/释放实现；台账保留该探针确切哈希。最终预检修复另由新构建探针的22段生成/两种边界回归与Core测试验证，不能把旧探针称为最终UI构建的20分钟验收。

## A5 Android实际交互结果

API36 / pixel_7_-_api_36_0 / emulator-5554。保留原中文UI、11条课文、150%字号、模型和067音色。

- 首页仍无顶部提示/工具，b/p/m/f点按无错误；m长按进入例词页，“妈妈 mā ma”直接点按、长按进入共用ReadingPage，显示“对母亲的称呼。”；截图确认局部m红色下划线，无独立点读按钮。点读操作和音源映射复核不代替人工发音听审。
- 更新重启后模型仍已安装、067已启用；普通中文试听显示“播放完成”。停用显示下载声音保留，再启用成功，没有重下载。
- 输入追加ABC后显示明确不支持提示，控件恢复可用，离页重入普通中文恢复；最终进程日志未发现OOV或AndroidRuntime/FATAL异常。
- 默认中文加20组`12345 `，开始生成后切后台，再返回显示“已停止”；重播、手动停止正常。再次重播后切学习页正常，没有设备失去响应。
- 飞行模式 enabled 且wifi_on=0，已存在的个人工程课文AI-voice-smoke全文朗读显示“播放完成”；恢复原网络airplane disabled、wifi_on=1、mobile_data=1。未新增/删除个人内容、未录音、未外发文字。
- 设备XML/截图/操作日志、媒体状态和进程日志留在私有TEMP/HanMate-stage-a-20260919。仅公开工程输入/摘要入仓库，原始设备材料不自动提交。

最终APK SHA256：`35e184a1c4d3b85e494ae08f1ee39ead61f440c5dcb4d1e823cb8ca7de255434`。
Windows应用程序集SHA256：`137b038c684c660ec173e3d4faac46564b74071dba3190b0572217c2424bf5c0`。

## Windows与其余边界

按computer-use技能枚举窗口、明确传入HanMate exe启动后，工具仍返回 `launched app did not expose a targetable window: C:\Users\yoiri\AppData\Local\Microsoft\OneDrive\OneDrive.App.exe`；刷新窗口仅见Visual Studio项目窗口，无可操作HanMate窗口。停止重复错误启动，没有借用其他程序窗口或把原生推理当UI通过。

A的本机修复、聚焦回归、Android交互、审核准备和台账已交付；Windows UI仍BLOCKED，真实设备/满盘/20分钟设备音频播放、默认音色及174音色人工听审仍NOT RUN，ong/许可仍OPEN。可以推进B的安全副本恢复向导与媒体清理，同时保留这些验收项，不能把A全部平台/正式内容门禁写成完成。
