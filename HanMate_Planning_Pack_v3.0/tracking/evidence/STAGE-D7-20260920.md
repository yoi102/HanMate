# D7：实际录音、权限和离线音频验证

2026-09-20。Windows/Android范围，iOS依ADR-105排除。本文按实际构建分开记录。W0-04与W3-05按原backlog交付条件结项，当前40 DONE / 6 IN_PROGRESS / 2 TODO；这是工作包进度，不是正式发布完成。

## 结项依据

| 工作包 | 原backlog交付条件 | 本轮依据 |
|---|---|---|
| W0-04 | 本地播放、真实录音、TTS离线状态 | Windows实际人声录音及试听确认、Android真机人声/离线本地播放、权限拒绝与恢复、不可用TTS提示；基本停止与生命周期已验。耳机/蓝牙/电话等完整设备路由矩阵仍属于W5质量门禁，不标本轮PASS。 |
| W3-05 | 录音试听重录保存、真实解码检查 | 两端录音保存和再录、试听/持久化/默认保护；Windows生产MP3/M4A复验与既有Android原生导入证据组合。 |

## Windows修复

实际未打包应用录音在采集前失败：MAUI Essentials 10.0.20的Microphone权限检查经EnsureDeclared、IsCapabilityDeclared读取运行目录AppxManifest.xml，原目录缺少该文件，抛FileNotFoundException（0x80070002）。同机生产NativeWaveRecorder独立采集成功，因此不是仅依据设备枚举猜测麦克风可用。通过本机已安装程序集反编译定位；没有改权限返回值或绕过系统检查。

HanMate.App.csproj在Windows且WindowsPackageType=None时，将已有Package.appxmanifest复制为AppxManifest.xml，同时CopyToOutputDirectory/CopyToPublishDirectory。源与最终发布清单SHA256均为e18f7f4acf9a4a0b70dcf83a3eebb8ba32f60f91a4b7a296bec78f6c024b4895。诊断只记录异常类型/HResult/堆栈，不输出异常消息或用户正文；录音与导入重抛保留原始堆栈。

修复构建实际应用录音33.44秒，用户明确回复“能，声音清楚”，试听完成后保存D7-Windows-Recording-1；第二轮16.84秒保存D7-Windows-Recording-2，第二轮仅验证重复采集流程，不算第二份真人听审。离页停止第三轮并留下27.2秒ready草稿。离页重入后两条已保存音轨仍在，工程基线D2 synthetic tone默认未变；新增录音均非默认。只读SQLite/实际WAV回读验证字节长度、SHA256、44100Hz/mono/PCM16、时长和引用。第一条录音信号幅度较低（峰值240/32768、全段RMS3.31），用户试听结论与信号数据分别保留，不扩大为录音增益或教学音质认证。

## Android实际设备

vivo V2408A / PD2408、Android16，USB序号仅保留私有日志。已安装Debug APK SHA256为9524c77c3bf6c0d5d500f72a8948045703b543a2bbcaf2ef9814aad44a1b21c2。覆盖安装Release曾返回INSTALL_FAILED_ABORTED（系统确认未允许），没有卸载或绕过；以下真机UI记录属于这个已安装APK，不套用最终新APK哈希。原生录音和本地音轨持久化路径保持原实现；本轮共享页面改动仅诊断与异常堆栈。

目标为随附“你好”完整单元。实际26.4秒录音试听获用户“试听清楚”确认，保存为7001；第二次19.6秒试听并保存7002，均为user/confirmed，未设默认。权限回收导致应用重启后重新进入目标检查持久状态。录音期间播放/录音/导入禁用；第三轮按Home切后台后留下46.0秒ready草稿，返回不自动恢复录音。

关闭Wi-Fi和移动数据（两值实查0）后，7002本地试听完成；随后恢复两项为1。断网时系统语音检测明确显示“未找到可用普通话语音，或系统语音服务未就绪”，试听禁用；这是不可用状态处理通过，不是Android系统TTS合成通过。Windows实际检测已安装普通话候选并试听固定句完成，走本机SpeechSynthesizer；未禁用电脑网络，不将其冒称飞行模式实测。

## 权限拒绝、恢复与资料核验

Android首次/再次系统授权弹窗由用户允许，这两次不计拒绝证据。随后ADB仅对HanMate执行pm revoke RECORD_AUDIO及临时user-fixed，形成确定拒绝状态；在真实应用连续两次点击录音，均显示“未获得麦克风权限。可在系统设置中开启，之后再点击录音”，未创建录音或重复弹窗。清除本次user-fixed并pm grant恢复原来权限，实查granted=true且仅原USER_SET/USER_SENSITIVE标志；同页再次录音成功，停止后14.7秒ready草稿。此为受控OS权限状态测试，不声称点击了系统拒绝按钮或Windows隐私总开关。

停止采集后暂时关闭HanMate，二进制安全读取私有数据库与两条已知测试录音，在私有TEMP只读核对：7001=26439ms/2332046字节、7002=19639ms/1732286字节，均44100Hz/mono/PCM16、user/confirmed、无default；各实际文件SHA256与资产行一致且非静音。保留两个后台/恢复测试草稿，重新启动应用。没有写入或替换设备数据库。

Wi-Fi=1、mobile_data=1已恢复；USB保持亮屏从临时2恢复原0，并再次核验。没有改变声音、模型、语言或阅读偏好。USB曾断开，用户重新连接后才完成拒绝测试及设置恢复，没有把断开前未执行的动作记PASS。

最终Windows发布产物另行实际录制12.7秒并停止形成ready草稿，stderr为空，证明CopyToPublishDirectory在发布目录也解决了录音权限清单错误；人声听审仍绑定前一修复构建，不扩写为最终产物再次听审。

## 自动验证与构建

- Core 29/29：PlaybackCoordinator、CompressedAudio、SpeechText。
- Infrastructure 25/25：LocalAudio、ReadingAudio。
- Windows生产MediaTranscoder和AudioFileImport：MP3→PCM16 WAV 1059ms、M4A→PCM16 WAV1044ms，均44100Hz/mono且非静音；截断MP3拒绝、取消不留草稿/解码目录、备份往返2音轨通过。Android压缩导入复用[既有平台证据](W3-W4-compressed-speech-migration.md)，不冒充本轮手机重做。
- Android最终Release构建0警告/0错误，APK SHA256 5343a6fe285dd41dae61fc221576b67542ab9a9ca15cbb082d4ae682ba1585da；本轮未安装到手机。
- Windows修复Release构建0警告/0错误；最终Release publish成功，发布清单与源文件哈希一致，发布应用实际启动无stderr。
- Windows人工试听构建DLL SHA256 da3dd9d6ea2f859adb7f5baf8c6e63d06ed994099bf10642dab75d9fe2695b90（Windows）；最终发布DLL7a0de714c391d5c405e99d69fcdac8cac22a26ce5017131e928e38b46a2788ff。最后差异为录音异常重抛保留堆栈。

## 文档与发布检查

规划包16组校验PASS，220文件哈希/完整文件集匹配；59补充场景、82分范围执行行有效，原176仍全部聚合NOT RUN。最终APK预检的权限、自动备份与分享范围PASS；发行身份、Windows publisher、剩余工作包、正式内容/音源审核及原生/商店完整证据5组仍BLOCKED。

## 证据与边界

原始UI、测试TRX、构建日志、原生探针和只读文件核验留在私有TEMP/HanMate-stage-d7；真实录音不复制进仓库。UI驱动支持精确卡片限定和可见控件去重/ScrollIntoView，动作失败后不继续依赖输入。Windows旧用户窗口未强关；未清库、替换资料或修改既有声音/模型偏好。

耳机拔插/蓝牙切换、电话打断、长时长/存储不足、Windows物理麦克风增益与独立断网TTS、真实读屏、所有适用P0和完整176矩阵仍在W5设备质量/回归门禁。HM-D019仍OPEN，正式音源/内容/许可及发行签名未关闭。真人短录音通过不等于教材发音或发行审核通过。
