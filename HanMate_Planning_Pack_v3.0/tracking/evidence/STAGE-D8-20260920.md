# D8 · 内部开发交付与例字配词 · 2026-09-20

## 范围和结项

用户明确“先完成内部开发交付，正式审校、签名和商店发布后置，并单列这些待办”，并要求测试先放一边。ADR-106保存范围变更；`tracking/internal-delivery-scope.json`保留原始正式验收行，`tracking/deferred-release-gates.json`保留7项未通过的正式门禁。iOS依ADR-105延期。

| 工作包 | 本轮实际交付 |
|---|---|
| W0-06 | 基于D6/D7平台结果锁定技术路线、.NET/MAUI/依赖/工作负载/JDK/SDK及5项目锁文件，记录替代路线与未关风险。 |
| W5-02 | 更新实际打包内容和声音清单、哈希及离线审阅入口；1,444对象全部待审，未生成虚假审核结论。 |
| W5-03 | locked restore、Windows Release publish及Android Release build；APK为现有Android Debug证书开发签名，未安装。 |
| W5-04 | 完成本地数据/录音、显式模型下载、系统TTS、分享及备份的数据流/隐私草案；源和编译APK权限/分享/备份声明检查PASS，正式商店资料后置。 |
| W5-06 | Windows ZIP、APK、源码ZIP、逐文件哈希、构建日志、离线审阅音频及安装/恢复指南；实际校验文件集合和两个ZIP全部条目。 |

当前45 DONE / 2 IN_PROGRESS / 1 TODO；W5-01/05/07保留。只增加内部交付完成数，不宣称正式发布或全量验收完成。

## 例字组成词

按用户追加要求新增42个开发例词，优先找含例字且读音/声调相同的词。目标可以在词中任意位置，界面按例字后接匹配词显示，保留原例词及稳定内容ID。例如八→八方、趴→趴下、坡→山坡、爬→爬山、火→火车、骑→骑马。

直接核对生成内容：63项、417例字词、394已有来源录音，256处例字中251处与目标高亮词匹配。未配齐：g/葛ge3、x/习xi2、o/噢o1、o/哦o2、o/哦o4；其中习与学共用“学习”，当前高亮学，不将其重复记为习已匹配。详细数据见[D8配词报告](D8-character-word-pairing.json)。

新增42词没有整词录音；缺失时不给wordAudioKey，不使用单字音频或音节拼接冒充整词。AI/原录音选择继续按原能力和提示。释义注音经上下文修正仍属待审稿，正式读音/日英译文与自然度未批准。

## 实际验证和产物

执行 `tools/build_internal_delivery.ps1` 到 `artifacts/internal-20260920-d8-v2`：locked restore PASS；Windows Release publish PASS；Android Release build PASS（0警告/0错误）。APK签名校验exit0，证书为CN=Android Debug，SHA256为 `c3c7a3eef634da8dd25ab97127c8215fda93b251a44709e73f972d6d4d649744`；apksigner对META-INF条目签名保护有警告，保留原始日志，不把它描述为正式分发审核。

初始 `delivery/` 的424交付文件及两个ZIP的全部条目长度、SHA256、路径集合校验PASS。最终状态文档随后重新归档至 `delivery-final/`；编译输入必须与构建前快照逐文件相同，管理文档可在构建完成后收口，应用不重编译也不更换代码。最终清单和哈希见同目录 `delivery-manifest.json`，机器证据见[验证记录](STAGE-D8-verification-20260920.json)。

内容审核材料核验PASS只说明清单与素材一致：440内容、882单元、394录音、63示范及174声音；正式审核对象共1,444且全部pending。默认库292,114条分293批，0条完成批次终审；394录音许可版本和词库分发权利未解决。

## 未执行与后置条件

应用自动测试、UI/新版本录音验收、新APK安装、旧数据迁移、正式签名及发布均NOT RUN。原176聚合NOT RUN及82执行行保持原文件哈希，不复用旧平台PASS冒充新版。HM-D019保持OPEN；没有操作手机、清数据、改权限或上传资料。

正式预检保持6组BLOCKED：release_identity、windows_publisher、work_packages、deferred_release_gates、shipped_content_and_audio_review、native_and_store_evidence。对应7项后置门禁独立跟踪；用户恢复测试后继续W5-01/05/07和RG-05/06，正式审校/签名/商店流程由RG-01/02/03/04/07推进。

## 最终归档核验

`delivery-final/` 已组装并验证：424个交付文件及两个ZIP的完整条目集合、长度和SHA256全部匹配；源码归档包含最终45项backlog及ADR-106原始条件快照。最终产物哈希见机器验证记录的 `finalDelivery`。原台账CSV逐文件SHA256与本轮收口前相等，82执行行及176聚合未改。规划文档16组检查PASS（含合成元数据检查，不属于应用测试）。
