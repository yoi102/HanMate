# 本机剩余项补齐：语音可靠性与性能 · 2026-09-19

## 已完成改动

- 长语音保留500 UTF-16限制，优先在句读、空白处分块，保持全文与组合字符完整；系统语音最多180元素，Android压力验证后AI进一步限为48元素，试听与正文一致。没有边界才按容量切块。
- 模型状态区分“校验可用”和“存在下载文件”，损坏模型仍可从设置删除；停用保持模型、跨实例选择状态与删除隔离有回归。
- 语音页防止离开后的下载进度/旧错误更新新页面，确认对话框返回时检查页面代次；状态刷新结束前保持互斥，取消和导航返回重新读取真实状态。音色选择器与试听文本增加三语言可访问名称和可见标签。
- 真实原生压力测试发现sherpa-onnx忽略并打印OOV字符。AI朗读现在在整队列播放前按固定词典验证，英文/表情/未知汉字给出明确提示，不静默漏读、不把这些字符送入原生日志。数字/全角数字与括号/空白停顿有确定规范化，原正文不变。系统语音仍独立确认，没有偷偷回退。

## 本机验证

- Core Release **218/218 PASS**、Infrastructure Release **245/245 PASS**，合计**463项**，没有跳过。TRX在开发机TEMP/HanMate-local-completion-20260919。新增10个Core用例和1个Infrastructure用例，覆盖词边界/Unicode完整、模型外输入拒绝、损坏模型删除与停用持久性。
- Windows生产推理 **174/174音色**均生成有效8kHz非静音PCM；4组中文/多音字/日期/全角数字括号输入可生成，4组不支持输入在原生调用前拒绝；实际中途取消及30次连续取消后再生成通过。完整结果见[原生合成报告](W5-voice-synthesis-audit-20260919.json)。信号检测不等于174音色听审，也不证明多音字读音正确。
- Windows最终Release零警告错误。Android最终构建和UI收尾结果见下节。

## 性能基线

Windows 10.0.26220、.NET 10.0.12、24逻辑处理器；每组预热1次、测量30次，无并行构建/压力任务。仅生产Core/SQLite服务，未计UI、250ms防抖或音频首音。字典测试为独立TEMP库的同词头最差候选排序夹具，不作为教材内容。数据见[原始30次样本](W5-local-performance-20260919.json)。

| 场景 | P95 | 边界 |
|---|---:|---|
| 1,000字注音 | 5.0ms | 本机服务时间 |
| 5,000字注音 | 27.4ms | 本机服务时间 |
| 20,000字注音 | 52.6ms | 文本完整，未知字仍待复核 |
| 20,000字阅读投影 | 26.1ms | 每页最多96原子；不是原生排版帧率 |
| 10,000词头查询 | 最高52.4ms | 5种查询各30次；低于初始200ms目标 |
| 100,000词头查询 | 最高424.2ms | 全部同词头压力场景；不承诺200ms |

本机词典冷加载150.7ms，探针全进程峰值工作集约181.6MiB。音色审计中的延迟仅诊断记录（部分与App构建并行），不作为可听首音或正式性能基线。

## 环境与素材仍缺的条件

- Windows computer-use本轮重试：明确启动HanMate.App.exe却返回`launched app did not expose a targetable window: ...OneDrive.App.exe`；重新列窗口仍无HanMate窗口。没有复用旧坐标或把构建标为Windows UI PASS。
- iOS按用户要求暂缓；ARM真机、真实麦克风/路由/长时、读屏/键盘完整验收及商店签名仍未具备完整证据。
- Commons对精确`Zh-ōng.ogg`/`Zh-ong.ogg`查询均返回missing；没有把dong替代ong。audio-cmn原README的SWAC许可链接`packs.shtooka.net/cmn-caen-tan/readme.txt`当前DNS失败，具体CC BY-SA版本仍未确认。正式听审/译文终审不能由编译或信号检查代替。
- W5-01进入IN_PROGRESS，DONE保持35/48。完整176用例、正式内容和发行门禁继续保留；未进行真实数据替换、公开发布或外发。

## 最终Android收尾

初版长数字试听（默认中文后追加20组`12345 `）一次停止成功，再次生成并离页时1.5GiB/API36模拟器连ADB shell也失去响应；Windows宿主仍可操作。未取得可确认OOM或泄漏的根因日志。没有清库；关闭本次模拟器并用`-no-snapshot-load`保留原userdata冷启动。随后缩小AI原生单次输入为48元素，Windows/Android最终Release均零警告错误，再次`adb install -r`成功。

修正后的同一长数字输入已经实际通过停止→重启生成→离页返回，仍可重新进入语音页并读取067选择；原模型保留、没有旧错误回写。普通中文试听完成，不支持英文的提示已在设备显示并检查截图；对应进程未发现OOV/崩溃日志。设备当前使用中文UI（本轮进入时即如此），未更改语言设置。

同一长数字文本最终逐块完整播放，页面显示“播放完成”，停止→再次生成→离页返回均通过；运行中PSS抽样约210–222MiB（不是严格峰值测量），设备与ADB保持响应。对应进程没有新增OOV/AndroidRuntime崩溃记录。最终截图在TEMP/HanMate-local-completion-20260919/long-completed.png；冷启动后原11条课文列表、AI-voice-smoke个人正文和150%阅读字号均仍存在。

最终APK为`HanMate.App/bin/Release/net10.0-android/com.companyname.hanmate.app-Signed.apk`，66,915,723字节，SHA256 `c0d54aba6c6fb57a7984fe22ca48c0fca69d6acd48abe3b74a02b4db9529e798`。517键三语言一致，最终权限/自动备份关闭/分享范围检查PASS，发布身份、签名和完整原生/正式内容验收仍BLOCKED。
