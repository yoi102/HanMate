# 字典释义/例句点读与内置语音 · 2026-09-20

## 实现

- 词头保留点击发音；释义段落和例句点击文字即可朗读，原生按钮承载键盘/读屏操作，不添加播放图标。视觉分页与折叠不切断段落语音；同段再次点击停止，切换和离页取消，录音互斥。
- 完整源单元的匹配录音优先，部分段落不能截取整单元音轨。辅助/替换“～”例句按显示正文合成。完整段落先预检不支持字符，避免先读半段再失败；不把展示拼音等同于全文明确音素合成。
- 8份既有AISHELL-3文件随应用内置，总32740562字节（31.2 MiB），SHA256与固定catalog一致；默认音色66，174种可选音色。许可/原始模型卡/署名随附，原始模型不改。
- 新voices目录首次使用本地解包、验证并启用默认音色。旧音色/停用/移除状态保留；初始化标记持久化取消后意图，清除缓存不会被重启自动恢复。显式恢复从安装包本地复制，不发网络请求。安装包中的原模型仍占空间，设置说明清除的是解包副本。
- 用户资料、收藏和音频不修改，SQLite及个人备份格式不变；模型/音色仍不随备份迁移。旧“仅下载后使用”规则由ADR-108更新。

## 验证

- PASS：57项Infrastructure测试（字典展示/点读目标、语音包状态与来源、安全安装/取消/校验、阅读音轨），含6项内置包新用例及3项段落目标新用例。
- PASS：26项Core播放协调/输入预检测试。
- PASS：生产Windows原生合成，直接从随附资源解包，新安装默认66号音色，禁止HTTP的处理器未收到请求；两段中文生成3.101秒/1.511秒、8kHz非静音PCM。验证不支持字符预检和停用后重启保留；未播放或进行真人听审。
- PASS：Windows Release，0警告/0错误。
- PASS：Android Release，0警告/0错误；Windows产物和APK内8份模型资源均逐一匹配catalog哈希，许可/署名文件随附。
- PASS：三语言各594键，键集与占位符一致。
- NOT RUN：设备/UI、实际点读和听审、新安装/升级实机验证。本轮未安装新APK、启动应用或改变设备数据/权限。

证据：仓库artifacts/dictionary-audio-20260920/中的TRX、构建日志、native-synthesis.log及probe源码。产物使用TEMP/HanMate-dictionary-audio-20260920/；不覆盖D8归档。文件哈希见changed-inputs.json。

当前45/48、176聚合NOT RUN、82条执行记录、HM-D019 OPEN及正式内容/许可终审、签名/发布后置边界不变。可生成音频不代表全部读音标准或正式发行完成。
