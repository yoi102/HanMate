# 手写例词换行和裁切修复 · 2026-09-20

用户提供的真机截图可见“三”“户”的例词第一行只有首字，第二个字只显示上半截；这是换行后垂直裁切的外观，并非缺少原词条文字。代码中FlexLayout允许横向压缩且使用多行原生Button，与该现象一致。未通过设备追踪断言唯一平台根因。

## 改动

- 新WordFlow按实际自然宽度排列整词，不压缩每个短词；行末不足时整词换行。
- 超过整行宽度的长词重新按受限宽度测量，使用真实多行高度；文字Label与透明可访问Button分开，保持原跳转及48逻辑像素最小触控高度。
- 尺寸向上取整以避免分数像素截断。布局响应可用宽度和字体缩放，未固定某个分辨率，也未缩小文字规避问题。
- 候选即时显示及逐行补全保持，未改变识别、语音、收藏或用户数据。

## 验证

- PASS：7项WordFlowTests，包括整词移行、120/240逻辑宽度、1倍/2倍模拟字体测量、超长词重测高度、分数尺寸及空列表。测试检查所有放置范围与行高；不代表原生字体/设备截图通过。
- PASS：Windows Release，0警告/0错误。
- PASS：Android Release，0警告/0错误。
- NOT RUN：用户这台手机的原生布局复验、新安装/升级及UI调试；遵循此前“先不调试”，未操作设备。

日志、TRX及源码哈希：仓库artifacts/handwriting-wrap-20260920/。新产物位于TEMP/HanMate-handwriting-wrap-20260920/。45/48、原176聚合NOT RUN与82执行行均不变。
