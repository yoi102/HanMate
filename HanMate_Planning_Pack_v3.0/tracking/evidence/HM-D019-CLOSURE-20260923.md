# HM-D019 结案决定 · 2026-09-23

2026-09-23 用户明确要求关闭 HM-D019 并继续完成 iOS 以外的工作。本记录将状态改为 **CLOSED_BY_REQUEST / residual risk documented**，不标记为 `FIXED`，不声称找到 Mono 原生 SIGSEGV 的根因。原始失败仍见 [D5 证据](STAGE-D5-20260920.md)：中间 APK 在长释义滚动时曾出现一次原生崩溃，日志与失败执行行保留。

结案时可复核的后续证据：稳定列表增删与释义分段已实施；先前 D5 的 2 轮 Android 聚焦路径未复现；2026-09-23 同一隔离 API 36 / 4 GB RAM 模拟器上的 3 轮与追加 5 轮“日”长释义展开、拼音显隐、滚动和收起均通过，追加 5 轮期间应用 PID 一直为 2097。结束后最近 8,000 行 logcat 未见 `SIGSEGV`、`Fatal signal`、本应用 `FATAL EXCEPTION` 或 ANR。Android 与 Windows 的原文返回保位另见 [字典返回证据](DICTIONARY-RETURN-20260923.md)。原始动作、UI XML/截图在 `%TEMP%\HanMate-dictionary-20260923\stress-closure-5\` 和前轮目录；UI 专用 APK 不含语音模型，不能用来验收声音或正式发布。

首次 2 GB 模拟器的第三轮曾遇 `uiautomator dump` 超时与模拟器内存/swap 耗尽，但应用进程仍在，不能把它计为应用原生崩溃。当前证据也不能排除其他设备、内存压力、长时间使用或旧中间 APK 特有的崩溃路径。若同类原生 SIGSEGV 再现，应立即重新登记缺陷，保留崩溃 tombstone、准确包哈希、设备/内存和动作序列。

本结案只移除 HM-D019 作为当前未关闭缺陷的状态；W5-01/05/07、真机/读屏、原 176 用例与正式内容、权利、签名和商店门禁仍按各自证据判定，不因本决定自动通过。
