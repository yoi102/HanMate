# 29 · 逐项验收用例

> HanMate / 汉语小伴 · v3.0 · 2026-09-14

共 176 项应用用例；预期和汇总状态真源为 `tracking/acceptance-cases.csv`。
本文件由 `tools/build_bundle.py` 生成。实际执行须另附平台证据；文档自检不提升应用状态。

## 语言与界面

### HM-T001 · 三语言循环切换
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：依次切换 zh-Hans、ja、en，检查已打开页面和新弹窗
期望：导航、按钮、提示立即切换，正文与拼音字节不变

### HM-T002 · 中文隐藏译文
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：在有日英译文的词条和课文切为中文
期望：不显示任何辅助译文，容器和留白同时移除

### HM-T003 · 缺日语译文
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：打开只有英语译文的个人词条并选择日语 UI
期望：不回退显示英语，正文正常，编辑页可补译文

### HM-T004 · 首次系统语言映射
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：分别以中文、日语、其他语言系统全新启动
期望：初始 UI 分别为 zh-Hans、ja、en

### HM-T005 · 保存用户语言
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：选择日语后更改系统语言并重启
期望：保留用户明确选择的日语

### HM-T006 · 语音语言独立
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：英语或日语 UI 点击中文例句朗读
期望：使用适用普通话音源，不按 UI 语言读中文

### HM-T007 · 键盘焦点
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：Windows 只用 Tab、Enter、Esc 打开和关闭弹窗
期望：所有核心操作可达，关闭后焦点回原触发控件

### HM-T008 · 200%文字缩放
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：各页面放大系统文字到 200%
期望：汉字拼音不截断，按钮可达，内容可滚动

### HM-T009 · 窄屏与横屏
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：在窄窗口、手机横屏打开词条和放大阅读层
期望：内容不重叠，不依赖固定像素宽度

### HM-T010 · 屏幕阅读器
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：逐项聚焦拼音、录音、收藏和播放按钮
期望：标签表达目标与状态，不重复读每个装饰元素

### HM-T011 · 红色非唯一提示
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：观察四声目标并使用无色感知或读屏
期望：同时有下划线/语义说明，目标仍可辨认

### HM-T012 · 列表返回保留
需求：R01;R02;R03；优先级：P0；层级：平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：筛选后滚动列表进入详情再返回
期望：保留筛选、查询和滚动位置，不随机回顶部

## 拼音教学

### HM-T013 · 拼音分组同页
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：滚动拼音页直到全部教学组
期望：声母、韵母等同页可达，没有遗漏类别

### HM-T014 · 条目正确示范
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：点击不同声母/韵母并核对音源清单
期望：播放对应教学音频，不用英文字母读法替代

### HM-T015 · 长按四声
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：长按有四声资料的 m 条目
期望：四行按一至四声纵向排列，显示例词及带调拼音

### HM-T016 · 长按不重复点播
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：从按下至长按触发后松手
期望：只触发长按行为，不额外播放条目点击音

### HM-T017 · 滚动取消长按
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：按下后拖动列表再松手
期望：正常滚动，不误弹四声层

### HM-T018 · Windows等价入口
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：用鼠标示例按钮或右键打开例词
期望：无需触屏长按也能进入四声示例

### HM-T019 · 局部拼音标红
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：查看 m 对应 mā 与 a 对应 ā 的例词
期望：仅数据指向的拼音范围高亮，不全局替换汉字或相同字母

### HM-T020 · 点击整行例词
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：点击四声行的文字而非小图标
期望：朗读整个例词，目标说明保持一致

### HM-T021 · 缺示例不编造
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：打开某声调没有已审核例词的条目
期望：明确暂无示例，不用轻声顶替、不制造词语

### HM-T022 · 缺教学音频
需求：R04;R05;R07；优先级：P0；层级：平台+内容；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：用开发坏素材夹移除一个示范音频
期望：正常提示不可播放且列为素材缺陷，不静默 TTS 冒充

## 学习与词条

### HM-T023 · 四种内容入口
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：切换词语、课文、语法、诗词
期望：各有对应真实仓储数据与空状态

### HM-T024 · 多维筛选AND
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：同时选择一个场景、难度和学段
期望：结果同时满足各维度条件

### HM-T025 · 同维多选OR
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：同一场景选择家庭与学校
期望：显示属于任一场景并满足其他维度的内容

### HM-T026 · 学段独立
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：选择未标学段与小学标签不同的资料
期望：未标注不自动当小学，难度不强制映射学段

### HM-T027 · 清除筛选
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：应用多个筛选后点清除
期望：恢复默认列表和标签状态

### HM-T028 · 同字不同义音
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：建立两个正文相同但读音/义项不同的词条
期望：具有不同 ID，搜索和详情不混淆

### HM-T029 · 词条头部播放
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：点击大字词条头部
期望：朗读该词条当前义项，不跳到其他同字词

### HM-T030 · 例句整句朗读
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：点击有逗号的例句
期望：朗读整个 example unit，不只读到首个逗号

### HM-T031 · 个人词条无例句
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：保存只有词头的个人词条
期望：能正常打开，无虚构自动例句

### HM-T032 · 内置编辑保护
需求：R06;R07;R08；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：尝试编辑内置正文或拼音
期望：提示复制个人版本，原目录内容不被写坏

## 注音阅读与分段

### HM-T033 · 基本Ruby对齐
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：显示学习词条和多行课文
期望：每字或合法儿化单元的拼音与正文成组换行

### HM-T034 · 不同宽度混排
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：显示长拼音、汉字、数字、英文和 Emoji
期望：使用真实测量，无空格凑齐造成的偏移

### HM-T035 · 逗号句号问号分段
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入今天天气很好，我们去公园。你要一起去吗？
期望：形成三个 speech 段，标点归前段，文本完整还原

### HM-T036 · 连续标点
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入真的吗？！好吧……
期望：连续标点作为边界组，无空播放段

### HM-T037 · 闭引号处理
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入他说：“你好！”然后走了。
期望：紧随终止标点的闭引号留前段，全部字符保留

### HM-T038 · 数字时间保护
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入3.14、10:30和版本1.2.3
期望：不在数字内部标点错误截断

### HM-T039 · 网址保护
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入含完整 URL 的句子
期望：不在 URL 内部按句点拆开，原文未改写

### HM-T040 · CRLF与空行
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入包含 CRLF 和空白行的诗文
期望：字符/换行守恒，布局段不触发语音

### HM-T041 · 枚举顿号
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入苹果、香蕉和梨。
期望：顿号不单独分出可点击段

### HM-T042 · 点击片段放大
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：点击课文一个小段
期望：同一放大层显示目标，播放到该段末即停止

### HM-T043 · 重复点击与切段
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：正在朗读时点同段，再点另一段
期望：同段停止；另一段替换声音，旧回调不改新高亮

### HM-T044 · 上下一段导航
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：在放大层使用上一段/下一段
期望：只切换显示，默认不自动发声

### HM-T045 · 全文朗读取消
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：开始全文朗读后退出阅读器
期望：后续片段不继续，相关音频和高亮结束

### HM-T046 · 长无标点正文
需求：R06;R09;R15；优先级：P0；层级：单元+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：打开20,000文本元素且无标点的正文
期望：保留一个合法语义段也能布局滚动，不静默截断或按视觉行改语义

## 离线字典搜索

### HM-T047 · 汉字精确匹配
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索你好
期望：对应已收录词条优先，未知资料不伪造释义

### HM-T048 · 无调分隔输入
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索ni hao
期望：召回你好等完整音节匹配词条

### HM-T049 · 无调连续输入
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索nihao
期望：可召回同样读音，稳定排序

### HM-T050 · 带调分隔输入
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索nǐ hǎo
期望：正确声调完整候选先于无调近似候选

### HM-T051 · 数字声调连续输入
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索ni3hao3
期望：与相应带调查询语义一致

### HM-T052 · 大小写与空格
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索带首尾空格的大写NI HAO
期望：规范化后结果一致，不改输入法中间状态

### HM-T053 · ü输入别名
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：分别搜索nǚ、nv、nu:
期望：对应 ü 候选一致

### HM-T054 · u与ü区分
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索nu并与nv比较
期望：不无条件等同，正确区分怒/女相关读音

### HM-T055 · 隔音符边界
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索xi'an
期望：西安等两音节匹配优先，不伪装成先的精确边界匹配

### HM-T056 · 连续歧义拼音
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索xian
期望：可召回先与西安，显示各自真实音节与声调

### HM-T057 · 轻声0和5
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：搜索同词的ma0与ma5形式
期望：均按轻声规范处理；无声调不是强制轻声

### HM-T058 · 查询迟到
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：快速输入三个查询并让第一个最后返回
期望：只显示最新 queryId 的结果

### HM-T059 · 底部框与IME
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入中文组合串并弹出键盘
期望：输入框可见，不频繁重写组合文本打断输入法

### HM-T060 · 索引随改音
需求：R10;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：修改个人词条读音并保存后立即搜索
期望：新索引原子生效，可重建，旧读音不作当前精确读音

## 收藏与设置

### HM-T061 · 收藏夹新增改名
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：新建、重命名、改说明后重启
期望：数据保留且标题合法

### HM-T062 · 空名和等价名
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入全空格名称及规范化后相同名称
期望：拒绝空名和普通夹重名，提示可理解

### HM-T063 · 多收藏夹同词
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：将同一词加入两个收藏夹
期望：两夹各有一次，逻辑内容只有一个

### HM-T064 · 重复收藏
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：同一夹重复添加同内容
期望：不产生重复关系

### HM-T065 · 删除一个收藏夹
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：删除包含多条内容的自建夹
期望：原文、音频和其他夹关系保留

### HM-T066 · 默认夹保护
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：更换语言后检查默认夹并尝试删除
期望：仍是唯一 systemRole 默认夹，不按名称重复创建

### HM-T067 · 收藏按钮多选
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：已在多个夹收藏时点收藏按钮
期望：展示成员选择；全部取消才取消全部收藏

### HM-T068 · 删除个人正文
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：删除有收藏与音轨引用的个人内容
期望：先提示影响并确认；合法级联和文件引用检查

### HM-T069 · 阅读设置恢复
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：改字号/拼音可见性后重启并恢复备份
期望：按数据库设置刷新，不被旧 Preferences 覆盖

### HM-T070 · 跨设备语音偏好
需求：R11;R12;R13；优先级：P0；层级：集成+平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：恢复含普通话偏好的备份到无同 voiceId 设备
期望：重新匹配可用语音，不保存无效设备ID阻断恢复

## 导入与注音编辑

### HM-T071 · 支持编码
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：分别导入UTF-8、UTF-8 BOM、UTF-16 LE/BE BOM的TXT
期望：正确解码，仅移除编码 BOM，内容与换行保留

### HM-T072 · 不支持编码
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入无BOM且不是合法UTF-8的文件
期望：明确拒绝/提示转换，不替换成乱码后保存

### HM-T073 · 文件与正文限额
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：分别超过1MiB TXT或20,000文本元素
期望：两种阈值独立检查，拒绝不截断，原输入可处理

### HM-T074 · 取消导入
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：在预览和注音阶段分别取消
期望：不产生半完成正式内容，临时资料可清理

### HM-T075 · 草稿恢复
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入未保存内容并按草稿流程退出重进
期望：恢复已成功落盘草稿，状态不冒充正式保存

### HM-T076 · 上下文多音字
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入银行、行走、音乐、快乐测试集
期望：结果对应已校对预期，未知规则有复核状态

### HM-T077 · 人工带调输入
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：将某字读音以lǜ或lv4保存
期望：规范存base=lü、tone=4、display=lǜ

### HM-T078 · 锁定重生成
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：锁定人工读音后再次自动注音
期望：保留锁定项，全部覆盖需预览确认

### HM-T079 · 插入原文
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：在校正字前插入文字并保存
期望：不按旧数组下标把读音套到别字

### HM-T080 · 重复片段歧义
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：重复句子之间插入删除并重分析
期望：不确定对应关系标记复核，不武断绑定旧录音

### HM-T081 · 未知字与Emoji
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：输入生僻字、Emoji、组合字符、繁体
期望：保留原文；非汉字不强行拼音；未知音与轻声区分

### HM-T082 · 并发编辑冲突
需求：R14;R15；优先级：P0；层级：单元+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：两编辑器基于相同rowRevision先后保存
期望：后提交获REVISION_CONFLICT并保留草稿，不按时钟覆盖

## 声音与录音

### HM-T083 · 单会话播放
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：连续点击多条本地音频
期望：始终一项播放，旧音源停止

### HM-T084 · 迟到完成事件
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：旧播放器取消后延迟触发完成
期望：不清掉新声音高亮，不继续旧全文队列

### HM-T085 · 录音与播放互斥
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：播放时开始录音
期望：先停止播放再实际录音，无扬声器任务残留

### HM-T086 · 录音中点击播放
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：正在录音时点词条
期望：不丢失录音，返回明确忙状态

### HM-T087 · 拒绝麦克风
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：首次拒绝录音权限
期望：不循环弹窗，其他学习功能可用

### HM-T088 · 录音保存链
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：录制、停止、试听、重录、保存
期望：实际可听有效音频，文件与所选目标一致

### HM-T089 · 录音中断
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：来电或应用进入后台
期望：状态真实，尽量保留可恢复草稿，不隐蔽续录

### HM-T090 · 录音上限
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：达到时长或字节上限
期望：达到先到阈值安全停止，草稿可试听处理

### HM-T091 · 格式与实际编码
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入扩展名正确但不可解码的音频
期望：拒绝并说明，不通过改扩展名伪装格式

### HM-T092 · 多音轨默认选择
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：录内置词条跟读并保存
期望：不默认覆盖标准示范；显式选择才切换

### HM-T093 · 拼音修改影响声音
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：修改有效读音后打开绑定音轨
期望：标记需复核，不宣称TTS自动遵循改音

### HM-T094 · 仅元数据修改
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：只改标题/分类/译文
期望：正文和读音未变的音轨保持适用

### HM-T095 · 只有整篇音频
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入整篇录音后点单段
期望：不平均分配时间戳；单段按独立音源或TTS处理

### HM-T096 · 普通话语音缺失
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：设备没有可用中文系统语音
期望：不读成其他语言，仍可阅读/录音

### HM-T097 · 严格本地模式
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：选择localOnly后播放无录音新文本
期望：不调用系统TTS，准确提示缺少本地音频

### HM-T098 · 分享单独录音
需求：R04;R08;R09;R16；优先级：P0；层级：平台+集成；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：对选定用户音频调用系统分享
期望：只开放选中文件，接收端可识别真实格式

## 分享备份与恢复

### HM-T099 · 内容包范围
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：分享一个内容并选择一条音轨
期望：只含选定内容闭包和音轨，不带设置/其他收藏

### HM-T100 · 完整备份范围
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导出含个人文本校正录音收藏的用户库
期望：所有已保存个人资料与用户音频依赖完整，草稿提示另行保存

### HM-T101 · 导出期间一致性
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导出时尝试修改内容和执行文件GC
期望：使用一致快照/文件租约，不出现文本和音轨混合版本

### HM-T102 · 保存取消
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：生成包后取消系统保存窗口
期望：显示取消而非已保存，不破坏本地资料

### HM-T103 · 清单和计数
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：修改counts或清单条目后导入
期望：拒绝实体计数/实际文件集合不一致

### HM-T104 · 包内文件哈希
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：篡改contents或音频一个字节
期望：写正式库前拒绝哈希不符

### HM-T105 · 同包幂等
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：连续两次导入完全相同包；删除一次导入条目后明确重新导入；编辑后再次用同备份执行替换恢复
期望：复用收据和映射，不增加重复派生内容；旧收据不阻止明确重装/再次恢复，每次执行身份独立；当前实体存在性和冲突重新检查

### HM-T106 · 相同包ID异内容
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：用相同packageId提供不同有效manifest
期望：拒绝PACKAGE_ID_COLLISION，不静默当新包

### HM-T107 · 内容ID冲突
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入同ID但正文或校正不同的内容
期望：保留双方，嵌套unit/token/segment/target关联完整重映射

### HM-T108 · 同名收藏夹冲突
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入不同ID但同名夹
期望：确定后缀保留双方，用户显式合并才合并

### HM-T109 · 默认收藏夹映射
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入另一语言创建的默认夹
期望：通过systemRole复用本机唯一默认夹

### HM-T110 · 合并设置策略
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：本机英语UI合并日语设置备份且未勾应用设置
期望：保持本机UI；显式选中后才应用备份偏好

### HM-T111 · 替换安全备份
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：执行完整替换并模拟预备份失败
期望：失败即禁止破坏性替换，原库保持有效

### HM-T112 · 预览计划过期
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：生成导入计划后修改本机数据再确认
期望：检测dataEpoch并要求重新预览，不执行旧计划

### HM-T113 · 提交前崩溃
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：正式音频安装后、SQLite提交前终止进程
期望：旧数据有效，新孤立文件可安全回收

### HM-T114 · 提交后崩溃
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：收据COMMIT后、外部日志更新前终止进程
期望：以收据为准识别成功，不错误回滚新数据

### HM-T115 · 路径穿越与重复
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入含../、盘符、符号链接或大小写重复路径的包
期望：写正式目录前安全拒绝

### HM-T116 · 资源与JSON攻击
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：导入过量entry、展开超限、深层JSON或重复属性包
期望：流式限额和严格解析拒绝，不耗尽内存或写坏库

### HM-T117 · 目录版本差异
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：恢复有内置快照但本机目录版本不同的包
期望：保留可读快照和音轨关联，缺目录音频准确说明

### HM-T118 · 音频去重与引用
需求：R13;R15;R16；优先级：P0；层级：集成+安全；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：同一文件多绑定，删除其中一项后清理
期望：不误删其他绑定/导出租约仍使用的物理文件

## 平台交付与可靠性

### HM-T119 · iOS到Android迁移
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：iOS导出基准完整包并在Android恢复试听
期望：设置、收藏、正文、锁定音和用户音频一致

### HM-T120 · Android到iOS迁移
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：Android导出基准完整包并在iOS恢复试听
期望：关联完整，无旧路径或设备语音ID依赖

### HM-T121 · iOS到Windows迁移
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：iOS导出并在Windows恢复，窗口内打开学习
期望：内容、读音、收藏和实际音频可用

### HM-T122 · Windows到iOS迁移
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：Windows导出并在iOS文件选择恢复
期望：字符范围、文件名与权限处理正确

### HM-T123 · Android到Windows迁移
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：Android导出并在Windows恢复
期望：内容和音频一致，无大小写路径冲突

### HM-T124 · Windows到Android迁移
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：Windows导出并在Android恢复
期望：通过流读取，无Windows绝对路径残留

### HM-T125 · 飞行模式闭环
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：断网浏览、搜索、导入TXT、自动注音、改音并播放本地音频
期望：核心本地能力可用，不暗接云服务

### HM-T126 · 数据库升级回归
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：用含用户资料的旧支持schema升级到新版本
期望：迁移失败回滚，成功后原校正/收藏/音频不丢

### HM-T127 · 搜索性能口径
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：10k词条在参考设备Release重复测量
期望：报真实P50/P95与样本；P95目标≤200ms且明确不含防抖

### HM-T128 · 满磁盘及重复操作
需求：R01;R13;R14;R16；优先级：P0；层级：端到端；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：已构建对应模块；使用独立测试资料，不覆盖真实用户库
操作：限制磁盘并反复打开关闭读录音与导入
期望：原资料保持、错误真实、无持续句柄或文件泄漏

## 语法与命名

### HM-T129 · 正式品牌一致
需求：R17;R02；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：依次切换三种UI，查看主标题、关于、导出来源说明
期望：中文名汉语小伴，英文HanMate；不再出现暂定工程代号

### HM-T130 · 四类型与预置语法
需求：R06;R07;R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：切换词语、课文、语法、诗词；打开语法样例
期望：四类均有真实持久数据和详情；草稿素材不伪称正式审核

### HM-T131 · 中文语法说明保留
需求：R03;R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：在语法页切中文、日文、英文
期望：中文说明/句型/拼音不改，辅助译文按选择显示，中文不留空白

### HM-T132 · 语法引用合法性
需求：R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：导入含跨内容或错误role unitId的grammar
期望：导入前拒绝GRAMMAR_REFERENCE_INVALID，正式数据不变

### HM-T133 · 句型占位符不误读
需求：R18;R16；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：点击句型结构并播放例句，检查实际音源请求
期望：不会把主语/地点/+作为例句交给TTS；音频target指向真实unit

### HM-T134 · 语法例句整句播放
需求：R08;R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：点击含逗号的一条例句
期望：整句朗读后停止，不沿用课文点段规则只读半句

### HM-T135 · 语法人工校正持久
需求：R15;R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：修改例句拼音锁定，重注音，保存重启
期望：同一出现位置保留锁定校正，其他说明例句不串改

### HM-T136 · 个人语法创建编辑
需求：R18;R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：新建句型、说明、一条例句，保存并修改标题
期望：真实持久化，title变化不使未变例句录音失效

### HM-T137 · 语法收藏删除保护
需求：R11;R18;R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：收藏资源语法并绑定用户录音，再卸载源包
期望：收藏可打开retained快照、录音不丢，正常学习列表撤下

### HM-T138 · 语法分类筛选
需求：R06;R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：组合场景/难度/学段筛选后清除
期望：同维度OR跨维度AND，未标学段不当小学，返回保留筛选

### HM-T139 · 语法内容包往返
需求：R18;R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：分享有校正的完整语法点到新库
期望：句型、说明、例句、所有unit引用与校正完整可编辑

### HM-T140 · TXT转语法草稿
需求：R14;R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：导入普通TXT并选择语法，不补句型或例句即提交
期望：原文进入说明草稿可保留；不伪造结构，不完整时拒绝正式提交

## 内容资源生命周期

### HM-T141 · 本地学习包安装
需求：R19;R22；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：导入含四种内容的合法资源包并重启；在基础文本安装阶段选择含音频包
期望：包登记、版本、内容映射、音频及索引一致，无需网络；音频能力未完成时整包拒绝，不允许删去音轨后标安装成功

### HM-T142 · 同包重复安装
需求：R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：同一resourceId/version/语义包导入两次
期望：不重复内容、索引或音轨；收据可追溯

### HM-T143 · 同版本不同载荷
需求：R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：修改同资源同版本的正文并重算清单后尝试安装
期望：拒绝RESOURCE_VERSION_COLLISION，旧版完整保留

### HM-T144 · 资源身份和类型保护
需求：R19;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：用字典类型更新学习包，或外包冒用内置保留ID
期望：拒绝种类改变和受保护ID碰撞，不凭publisher字段授权

### HM-T145 · 禁用与卸载区别
需求：R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：禁用学习资源再启用，并检查空间显示
期望：列表/搜索立即更新；禁用不宣称已释放文件空间，收藏仍可读

### HM-T146 · 资源单条撤下持久
需求：R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：撤下某一资源词语/课文/语法/诗词并重启
期望：该entry普通列表不可见，其余条目正常，撤下标记持久

### HM-T147 · 更新不复活撤下项
需求：R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：撤下entry后安装更高资源版本
期望：同entry仍被撤下，其他更新显示，个人副本不被覆盖

### HM-T148 · 卸载引用保护
需求：R11;R16;R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：收藏条目且有用户音轨，预览后默认卸载；另含被四声例词与高亮引用的条目
期望：受引用内容快照和目标ID保留，用户音频不被级联删除；教学入口仍可读取例词/正确高亮/审核音源，普通列表撤下，保留占用可解释

### HM-T149 · 资源删除不伤个人副本
需求：R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：复制资源内容为个人版并修改，再卸载源包
期望：个人原文/校正/音轨保持；资源新版本不覆盖该副本

### HM-T150 · 资源事务失败恢复
需求：R13;R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：在安装文件后/SQLite提交前注入错误和磁盘不足
期望：旧资源/查询完整，新孤立文件可清理，无半安装成功状态

### HM-T151 · 未来下载边界
需求：R22；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：飞行模式安装本地资源，检查依赖和网络日志
期望：统一安装服务可用，无账户/商城/下载服务器前置和隐性网络请求

### HM-T152 · 重装和恢复条目
需求：R19；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：卸载后重装相同资源，恢复特定撤下项；卸载随附包后重启并模拟应用目录升级，再显式重装
期望：同版复用正确身份和依赖，不生成重复收藏；仅恢复选中entry；启动/升级不自动复装，重装比较只规范化 retained 所有权标签并校验原身份，撤下偏好保留

## 字典管理

### HM-T153 · 字典本地安装
需求：R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：从P14导入合法handict，汉字/拼音查询
期望：资源登记正确，词条带来源，kind均为word

### HM-T154 · 字典启停生效
需求：R10;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：查询一个仅此字典有的词，禁用后继续查询再启用
期望：禁用候选消失、启用恢复，开关写入失败不假生效

### HM-T155 · 最后字典卸载与注音隔离
需求：R14;R15;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：卸载所有查询字典，导入新文本并查看既有锁定拼音
期望：基础自动注音正常，锁定校正不丢，个人学习词仍可查

### HM-T156 · 多字典优先级持久
需求：R10;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：调整两个字典顺序、重启、查相同精确词
期望：同匹配层级按持久来源顺序，不随机变化

### HM-T157 · 同字不同音义保留
需求：R10;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：安装两个含不同读音/定义的同字词条来源
期望：独立身份/来源可见，不把定义或音轨混合覆盖

### HM-T158 · 字典拒绝非词条
需求：R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：将grammar内容装入dictionary描述并校验
期望：Schema/业务验证拒绝，数据库约束辅助防护，不半安装

### HM-T159 · 来源改变丢弃旧查询
需求：R10;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：发起慢查询和分页后禁用其字典
期望：旧query/epoch结果不能回写，旧游标提示重查

### HM-T160 · 字典卸载保留收藏录音
需求：R11;R16;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：收藏并录音某字典词条，然后卸载字典
期望：收藏打开只读历史来源，自己的音轨可用，不参与普通搜索

### HM-T161 · 匹配等级优先来源
需求：R10;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：高优先级字典只有包含命中，低优先级有精确命中
期望：精确命中仍在前，来源优先级不越过语义等级

### HM-T162 · 字典设置离线恢复
需求：R13;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：备份有启停/优先级/撤下的多个许可允许字典到新库
期望：来源数据与用户管理状态一致，查询结果可复现

### HM-T163 · 字典中文释义不隐藏
需求：R03;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：打开有definition及日英翻译的词条，切换三语言
期望：中文释义固定保留，只有辅助译文随设置或隐藏

### HM-T164 · 不支持字典格式提示
需求：R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：选择任意MDX或改后缀但非合法HanMate包的文件
期望：清楚说明格式不支持，不执行/误读，不声称所有字典可用

## 分享与兼容迁移

### HM-T165 · 四类单条分享
需求：R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：分别分享word/text/grammar/poem并校验导入
期望：四类均保留必要正文、拼音和元数据，进入正确详情

### HM-T166 · 多选混合分享
需求：R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：同时选词语、课文、语法、诗词各一项
期望：生成一个包恰好含所选完整内容及依赖，不带未选项

### HM-T167 · 音轨选择和默认引用过滤
需求：R16;R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：条目有多条私人音轨，只选其中一条分享
期望：仅该音轨字节/绑定可见，未选preferences不悬空或泄露

### HM-T168 · 单独录音分享
需求：R16;R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：真实录音停止写完后分享文件，用独立播放器打开
期望：声音可听、后缀编码一致，不需HanMate；不假称含注音

### HM-T169 · 中文显示不删译文数据
需求：R03;R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：中文UI分享同时含日英译文的条目，接收方切日语
期望：原译文仍存在，发送UI仅控制显示不是数据过滤

### HM-T170 · 受限素材分享阻断
需求：R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：选择不允许再分发的文本/音频与本人录音混合
期望：明确阻断受限载荷，不以快照绕过；合法单录音单独处理

### HM-T171 · 分享隐私白名单
需求：R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：检查内容包所有entry与JSON键、模拟夹带settings/resources
期望：无未选资料/设置/字典状态；违规管理文件直接拒绝

### HM-T172 · 语法冲突全图重映射
需求：R18;R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：同ID不同语法内容包导入，包含audio target
期望：派生content/unit/token/segment及grammar引用全部一致

### HM-T173 · 三类包入口分派
需求：R13;R20;R21；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：从分享入口选到backup或resource，检查确认页面
期望：显示正确用途，content不能替换用户全局数据或安装/改字典

### HM-T174 · 新协议完整备份往返
需求：R13;R18;R19;R20；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：带语法、资源/字典、收藏、校正、用户录音完整备份恢复；携带标准资源音频并调整用户默认音轨
期望：所有应含数据与状态一致；实际音频要三平台试听才PASS；发布载荷指纹不受用户偏好影响，可备份资源标准音频不一律排除

### HM-T175 · 部分资源备份缺失提示
需求：R13;R20;R22；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：资源许可限制形成referenceOnly，离线新机恢复
期望：导出/导入明确缺失；未安装不启用、不自动联网；个人资料可用

### HM-T176 · 旧v1包显式升级
需求：R13;R15;R18；优先级：P0；层级：集成/平台；平台：iOS;Android;Windows；状态：**NOT RUN**。
前置：对应模块已实现；使用隔离测试库与明确标为测试的资源，不操作真实个人资料
操作：以archive/v2_baseline旧包校验并转换再导入
期望：原文/ID/范围/校正/收藏/音轨不改；补空资源登记不卸载本机源
