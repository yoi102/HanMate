# 历史文件与兼容夹具

v1.0原始需求保留在本目录。v2_baseline保留上一轮完整规划、原协议1 Schema、两个原包、原内容与原校验器，供显式兼容测试使用。

这里不作为当前实现真源，不把旧版“暂定名称/三类内容/协议1”与v3新规则混用。当前规范在docs、specs、AGENTS及更新摘要中。

旧归档内相对链接按历史上下文保留，不承诺全部指向当前拆分目录。归档不是应用备份，也不包含用户真实数据。
